---
gitlab:
  hostname: "gitlab"
  url_with_protocol: "http://gitlab.example.com"
  ports:
    http: 80
    https: 443
    ssh: 22
  user: "gitlab"
  working_dir: "/gitlab"
  backup_dir: "/backup/gitlab"
  backup:
    hour: 0
    minute: 0
